# Contributing

1. **Skill behavior** → edit `SKILL.md` + `references/`.
2. **Visual tokens** → `references/ui-tokens.md` (keep Lab·Canonical as default).
3. **Docs** → `README.md` / `README.zh-CN.md` / `docs/`.

Keep generated `web/*.html` out of PRs unless **sanitized examples**; `.gitignore` ignores them by default.
